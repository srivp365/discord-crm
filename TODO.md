~~1. Implement Pinging user~~
~~2. add a dropdown/auto complete section for the tier so that users don't have to spell (aka spelling on the go is hard)~~
3. Add additional note functinality
4. Fix the db row reads on birthdays, since using strftime forces the db to read every row and run the conversion instead of just fetching the rows we need.
5. Implement dynamic user pinging
6. Add a nicer response body from the bot
