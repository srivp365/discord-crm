[project]
name = "discord-crm"
version = "0.1.0"
description = "A contact/relationships management system, built on top of Discord"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "cryptography>=49.0.0",
    "libsql>=0.1.11",
    "py-cord>=2.8.0",
    "python-dotenv>=1.2.2",
]
