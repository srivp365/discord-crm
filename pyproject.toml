[project]
name = "discord-crm"
version = "0.1.0"
description = "A contact/relationships management system, built on top of Discord"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "dotenv>=0.9.9",
    "libsql>=0.1.11",
    "py-cord>=2.8.0",
]
